# Dependances optionnelles - installation best-effort :
#   pip install -r requirements-optional.txt || true
#
# python-evtx : parsing des journaux Windows EVTX.
# Sa dependance "hexdump" echoue a compiler avec les setuptools recents
# (Python 3.11+ / pip 24+). Sans ce paquet, l'upload EVTX retourne une
# erreur explicite mais tout le reste de la plateforme fonctionne.
python-evtx

# CLI legacy (legacy/phoenix.py) : typer pour l'interface, pandas pour le
# parsing CSV avance. Le fournisseur IA principal est GitHub Copilot (API
# GitHub Models) qui n'utilise que 'requests' (deja dans requirements.txt) :
# il suffit de definir GITHUB_TOKEN (ou PHOENIX_GITHUB_TOKEN, scope models:read).
typer
pandas

# Fournisseurs IA de repli (optionnels si GitHub Copilot est configure)
ollama
google-generativeai
