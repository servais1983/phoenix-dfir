flask==2.3.2
flask-cors==4.0.0
flask-socketio==5.3.4
python-socketio==5.8.0
werkzeug==2.3.6
requests>=2.31.0

# Parsing EVTX et providers IA : voir requirements-optional.txt (best-effort).
# python-evtx depend de hexdump, qui ne compile plus avec les setuptools
# recents ; le backend fonctionne sans (parse_evtx renvoie une erreur explicite).

# Production server
gunicorn>=21.2.0
eventlet>=0.35.0

# Observability
prometheus-client>=0.19.0

# Cache & rate limiting distribue (optionnel mais recommande en prod)
redis>=5.0.0
